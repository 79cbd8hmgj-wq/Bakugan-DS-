name: Milestone 6E Task 10 sync

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

concurrency:
  group: milestone-6e-task10-sync-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  sync:
    if: >-
      github.head_ref == 'milestone-6e-complete-gate-roster-conversion' &&
      github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install project
        run: python -m pip install -e '.[dev]'
      - name: Regenerate and verify Task 10
        shell: bash
        run: |
          set -euo pipefail
          python .github/scripts/m6e_task10_contracts.py
          python -m pytest -q \
            tests/unit/test_gate_roster_contracts.py \
            tests/unit/test_gate_milestone_6e_risk_chaos.py \
            tests/unit/test_gate_roster_metadata.py \
            tests/unit/test_gate_roster_analysis.py
          python -m compileall -q src tests tools
          python -m ruff check \
            .github/scripts/m6e_task10_contracts.py \
            src/bakugan_ds/gates/roster_contracts.py \
            tests/unit/test_gate_roster_contracts.py \
            tests/unit/test_gate_milestone_6e_power_attribute.py \
            tests/unit/test_gate_milestone_6e_skill_control.py \
            tests/unit/test_gate_milestone_6e_comeback.py \
            tests/unit/test_gate_milestone_6e_risk_chaos.py \
            tests/unit/test_gate_roster_metadata.py
          python -m mypy src/bakugan_ds/gates/roster_contracts.py
          python -m pytest -q
          git diff --check
      - name: Persist verified files atomically
        env:
          GITHUB_TOKEN: ${{ github.token }}
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha }}
          TARGET_BRANCH: ${{ github.head_ref }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from __future__ import annotations

          import base64
          import json
          import os
          import urllib.error
          import urllib.parse
          import urllib.request
          from pathlib import Path
          from typing import Any

          repository = os.environ["GITHUB_REPOSITORY"]
          token = os.environ["GITHUB_TOKEN"]
          branch = os.environ["TARGET_BRANCH"]
          expected_head = os.environ["EXPECTED_HEAD"]
          api_root = f"https://api.github.com/repos/{repository}"
          update_paths = (
              "analysis/gates/milestone-6e-roster-contract.json",
              "analysis/gates/milestone-6e-balance-contract.json",
              "config/gates/milestone-6e-system2-v1.json",
              "config/gates/milestone-6e-roster-metadata.json",
              "tests/unit/test_gate_milestone_6e_power_attribute.py",
              "tests/unit/test_gate_milestone_6e_skill_control.py",
              "tests/unit/test_gate_milestone_6e_comeback.py",
              "tests/unit/test_gate_milestone_6e_risk_chaos.py",
              "tests/unit/test_gate_roster_metadata.py",
          )

          def api(
              method: str,
              path: str,
              payload: dict[str, Any] | None = None,
          ) -> dict[str, Any]:
              request = urllib.request.Request(
                  api_root + path,
                  data=(
                      None
                      if payload is None
                      else json.dumps(payload).encode("utf-8")
                  ),
                  method=method,
                  headers={
                      "Accept": "application/vnd.github+json",
                      "Authorization": f"Bearer {token}",
                      "Content-Type": "application/json",
                      "User-Agent": "bakugan-milestone-6e-task10-sync",
                      "X-GitHub-Api-Version": "2022-11-28",
                  },
              )
              try:
                  with urllib.request.urlopen(request) as response:
                      return json.loads(response.read().decode("utf-8"))
              except urllib.error.HTTPError as exc:
                  body = exc.read().decode("utf-8", errors="replace")
                  raise RuntimeError(
                      f"GitHub API {method} {path} failed: {exc.code} {body}"
                  ) from exc

          encoded_branch = urllib.parse.quote(branch, safe="")
          ref = api("GET", f"/git/ref/heads/{encoded_branch}")
          current_head = str(ref["object"]["sha"])
          if current_head != expected_head:
              raise RuntimeError(
                  f"branch advanced: expected {expected_head}, found {current_head}"
              )
          parent = api("GET", f"/git/commits/{current_head}")
          tree_entries: list[dict[str, Any]] = []
          for path in update_paths:
              blob = api(
                  "POST",
                  "/git/blobs",
                  {
                      "content": base64.b64encode(Path(path).read_bytes()).decode(
                          "ascii"
                      ),
                      "encoding": "base64",
                  },
              )
              tree_entries.append(
                  {
                      "mode": "100644",
                      "path": path,
                      "sha": blob["sha"],
                      "type": "blob",
                  }
              )
          tree = api(
              "POST",
              "/git/trees",
              {"base_tree": parent["tree"]["sha"], "tree": tree_entries},
          )
          commit = api(
              "POST",
              "/git/commits",
              {
                  "message": "feat: freeze final Gate roster contracts",
                  "parents": [current_head],
                  "tree": tree["sha"],
              },
          )
          api(
              "PATCH",
              f"/git/refs/heads/{encoded_branch}",
              {"force": False, "sha": commit["sha"]},
          )
          print(f"Task 10 atomic commit: {commit['sha']}")
          PY
